# nkn-ms.github.io

制作物をまとめたページのソースです。<https://nkn-ms.github.io> で公開しています。

- 静的な HTML 1 枚（`index.html`）のみ。ビルド不要
- `main` ブランチへの push が GitHub Pages にそのまま反映されます

## ローカルで確認する

```bash
python3 -m http.server 8000
# http://localhost:8000 を開く
```

ファイルを直接ブラウザで開いても（`open index.html`）確認できます。
